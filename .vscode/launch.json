{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with Node",
            "type": "node",
            "request": "launch",
            "program": "${file}",
            "skipFiles": [
                "${workspaceFolder}/node_modules/**/*.js",
                "<node_internals>/**/*.js"
              ]
        }
    ]
}